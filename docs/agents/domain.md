# Domain Docs

This repository uses a multi-context layout.

- **Context Map**: `CONTEXT-MAP.md` at the root points to all context-specific directories.
- **Context Files**: Each context directory contains its own `CONTEXT.md` and `docs/adr/` directory.
- **Vocabulary**: Use the terms defined in the relevant `CONTEXT.md` for that specific area of the codebase.
