<!-- version-type: minor -->

# @furystack/filesystem-store

## ✨ Features

- Added `onWatcherError` event — emitted when the file system watcher fails to register, replacing silent error swallowing
- Added `onListenerError` to the event map for consistent EventHub error handling
