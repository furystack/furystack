<!-- version-type: patch -->

# @furystack/yarn-plugin-changelog

## 👷 CI

- Raised the minimum supported Node.js to `>=24.0.0` (Node 24 LTS) in `engines`, dropping Node 22.

## ⬆️ Dependencies

- Bumped `@yarnpkg/cli` to `^4.16.0` and dev `vitest` to `^4.1.8`. No source changes — dependency bump only.
