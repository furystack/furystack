<!-- version-type: patch -->

# @furystack/shades-showcase-app

## 👷 CI

- Raised the minimum supported Node.js to `>=24.0.0` (Node 24 LTS) in `engines`, dropping Node 22.

## ⬆️ Dependencies

- Bumped dev `vite` to `^8.0.16` and `vitest` to `^4.1.8`. No source changes — dev-tooling bump only.
