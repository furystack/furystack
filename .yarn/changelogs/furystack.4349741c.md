<!-- version-type: patch -->

# furystack

## 🔧 Chores

- Workspace repository patch release to ship the nested-router API extensions (`query` / `hash` schemas, `defineNestedRoutes`, `createNestedHooks`) and the `href` → `path` rename across `@furystack/shades`, `@furystack/shades-common-components` and the showcase app. See individual package changelogs for details.
