<!-- version-type: patch -->

# @furystack/cache

## 📚 Documentation

- Rewrote JSDoc on `Cache`, `CacheSettings`, and `CacheResult` to follow the new value-test guidance: dropped restate-the-type narration, tightened wording on `getKey` / `setExplicitValue` / `getObservable` semantics, and added an `@example` for `Cache` showing the `using(...)` + `subscribe(...)` flow.

## ⬆️ Dependencies

- Bump dev `vitest` to `^4.1.5`.
