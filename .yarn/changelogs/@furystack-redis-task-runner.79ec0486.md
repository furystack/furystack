<!-- version-type: patch -->

# @furystack/redis-task-runner

## ⬆️ Dependencies

- Bumped `redis` to `^6.1.0`, dev `@aws-sdk/client-s3` to `^3.1083.0`, and raised dev `typescript` to `^7.0.2`, `@types/node` to `^26.1.1`, and `vitest` to `^4.1.10`. No source changes — dependency bump only.
