<!-- version-type: patch -->

# @furystack/auth-google

## 📚 Documentation

- Rewrote JSDoc across the public API (`GoogleAuthenticationSettings`, `GoogleLoginService`, the client helpers) to follow the new value-test guidance: dropped restate-the-type narration, kept intent / trade-offs / constraints, and added type-only imports for cross-file `{@link}` targets.

## ⬆️ Dependencies

- Bump dev `vitest` to `^4.1.5`.
