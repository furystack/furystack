<!-- version-type: patch -->

# @furystack/rest-service

## ⬆️ Dependencies

- Bumped `ws` from `^8.20.0` to `^8.20.1` (dev dependency used in tests).
- Bumped `@types/node` to `^25.9.1` and `vitest` to `^4.1.7`. Dev-tooling only.
