<!-- version-type: patch -->

# @furystack/s3-blob-store

## ⬆️ Dependencies

- Bumped `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner` to `^3.1083.0` and raised dev `typescript` to `^7.0.2`, `@types/node` to `^26.1.1`, and `vitest` to `^4.1.10`. No source changes — dependency bump only.
