<!-- version-type: patch -->

# @furystack/entity-sync-client

## 📚 Documentation

- Rewrote JSDoc on `EntitySyncService` and the Shades hooks to follow the new value-test guidance: dropped restate-the-type narration, kept intent / trade-offs / constraints, and added type-only imports for cross-file `{@link}` targets.

## ⬆️ Dependencies

- Bump dev `vitest` to `^4.1.5`.
