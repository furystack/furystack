<!-- version-type: patch -->

# @furystack/auth-google

## 🧪 Tests

- Updated test setup to register `DefaultSession` and `PasswordCredential` stores required by the new `useHttpAuthentication()` provider resolution
