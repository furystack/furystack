import { Shade, createComponent } from '@furystack/shades'

export const TodoApp = Shade({
  shadowDomName: 'shade-app-todo-app',
  initialState: undefined,
  render: () => {
    return <div>TodoApp - ToDo:Implement</div>
  },
})
