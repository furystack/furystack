{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ],
    "nohoist": []
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "lerna": "^3.8.1",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1",
    "tslint-react": "^3.6.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-consistent-codestyle": "1.15.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "clean:packages": "lerna clean",
    "test": "jest",
    "start": "yarn workspace @furystack/example-repository run start"
  },
  "name": "furystack"
}
