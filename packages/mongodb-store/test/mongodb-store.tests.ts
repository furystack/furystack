import { Injector } from '@furystack/inject'
import { using } from '@furystack/utils'

describe('MongoDB Store', () => {
  it.todo('Should be activated and disposed', () => {
    using(new Injector(), i => {
      // ToDo
    })
  })
})
