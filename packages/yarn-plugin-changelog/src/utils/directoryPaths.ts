/**
 * Directory path for changelog drafts (relative to project root)
 */
export const CHANGELOGS_DIR = '.yarn/changelogs' as const;

/**
 * Directory path for version manifests (relative to project root)
 */
export const VERSIONS_DIR = '.yarn/versions' as const;
